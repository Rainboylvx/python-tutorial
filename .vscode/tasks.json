{
    // See https://go.microsoft.com/fwlink/?LinkId=733558
    // for the documentation about the tasks.json format
    "version": "2.0.0",
    "tasks": [
        {
            "label": "build and push",
            "type": "shell",
            "command": "mkdocs build && rsync -r --progress --delete ./site/ /vol1/1000/docker/baota/website_data/192.168.0.216/",
            "problemMatcher": [],
            "group": {
                "kind": "build",
                "isDefault": true
            }
        }
    ]
}